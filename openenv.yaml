spec_version: 1
version: "0.2.0"
name: SupportAutoEnv
type: space
runtime: fastapi
app: server:app
port: 7860
description: "A customer support automation environment for OpenEnv. 9 tasks across classification, entity extraction, and resolution planning. REST + WebSocket API with programmatic + LLM grading."
action: SupportAction
observation: SupportObservation
metadata:
  domain: customer_support
  author: Lax024
  model: meta-llama/Llama-3.1-8B-Instruct
  tags: ["openenv", "customer-support", "nlp", "meta-llama"]
